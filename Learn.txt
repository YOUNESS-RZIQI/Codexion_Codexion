
1) grab_dongles

    1-1) take_dongle : insert+setpraorty -> check_take_dongle: it does take dongle or wait until it does.
    1-2) set_priority_and_insert: set priority -> heap_insert.
            1-2-1) heap_insert -> heapfy_up -> compaire -> swap




    1-3) check_take_dongle
            1-3-1) heap_peek : it return the heap[0] node.
            1-3-2) heap_extract_min : 
            1-3-3) get_timespec_from_ms : this one is for condtimewait ?




2) release_dongle : set available to 1, add the coldawn, broadcast the else statment.

3) print_action : nedd only sime since start + coder id + action (message).

4) custom_usleep : 




Today : 

  1) heap.h                   | Not Done |
  2) heap_utils.h             | Done |
  3) when the stop_simulation = 1 ?
  4) the cond_time_wait why it exist ?

  5) why it bourn out when i delete : 
  	if (coder->coder_number % 2 == 0)
		custom_usleep(1, sim);

  5) what about : ./codexion 5 10 0 0 0 0 0 edf ?





Questions of Teorique : 

1) why the dongels also have mutex, cond wait  and sim has it own one ? 
        when i do need mutex for sim examplesand when for the dongels ?
        is when you do mutex to sim stops everything and the dongles mutes does not do that it only lock the two threads ?
        i need to know where each mutex and cond used in ? 
        sim... , print... , dongle...


2) the parametters of the pthreads.
